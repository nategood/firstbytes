extends main

block content
    style.
        @import url("/css/auth.css");
        @import url("/css/form.css");
    div#error(data-bind="text: err, visible: err")
    nav#menu
        ul
            li
                input#projectname(placeholder="Project Name", data-bind="value: projectname")
            //- li#lastsaved Last saved
            li.button#save Save
            //- li.button#revert Revert
            //- li.button#screenshot Snap a Screenshot TODO
            //- li.button#signup-button Sign Up Implicit only
            li.button#login-button(data-bind="visible: authenticated") Sign In
            li.button#logout-button(data-bind="visible: unauthenticated") Sign Out
            li.button#projects-button(data-bind="visible: unauthenticated") Projects
            li(data-bind="text: username") --
    iframe#sandbox(class="sandbox", src="/canvas", width="500", height="500", scrolling="off")
    div#editor(class="editor").
        // Welcome to FirstBytes! You are looking at your first "source code".
        // The lines with these funny "//" slashes in front are called "comments"
        // and are ignored by the computer. The other lines are instructions for
        // the computer to follow. Let's dig in!

        // Choose a blue background
        FB.background(FB.BLUE);

        // Pick up a white paint brush
        FB.fill(FB.WHITE);

        // Paint a rectangle (will use your brush you just picked up)
        FB.rect(10, 50, 100, 200);

        // What's next?
        // 1. Change the numbers above! For example,
        // make change 10 to 250. Can you you figure out
        // what the numbers do?
        // 2. Change the colors. You can try RED, GREEN, BLUE,
        // BROWN, ORANGE, PURPLE, PINK, YELLOW, BLACK, and WHITE.
        // 3. Try to add another rectangle. Copy and paste
        // the rect line and play with the numbers.
        // 4. Now that you have multiple rectangles, make one rectangle
        // green and one rectangle yellow.
        // 5. Can you make a face using multiple rectangles?
    div#login
        div.error
        h1 Sign In
        form(id="login-form", method="post", class="simple", action="#")
            input(name="email", placeholder="Email", autocomplete="off")
            input(name="password", placeholder="Password", autocomplete="off", type="password")
            button(type="submit") Sign In
        h3 or
        h1 Create Account
        form(id="signup-form", method="post", class="simple", action="#")
            input(name="name", placeholder="Name", autocomplete="off")
            input(name="email", placeholder="Email", autocomplete="off")
            input(name="password", placeholder="Password", autocomplete="off", type="password")
            button(type="submit") Sign up
    div#projects.modal
        h1 Projects
        ul(data-bind="foreach: projects")
            li(data-bind="text: name")

    script(type="text/javascript", src="/js/dom/appvm.js")
    script(type="text/javascript", src="/js/auth.js")
    script(type="text/javascript", src="/js/signup.js")
    script(type="text/javascript", src="/js/repo.js")
    script(type="text/javascript", src="/js/dom/editor.js")
    script(type="text/javascript", src="/js/dom/user.js")
